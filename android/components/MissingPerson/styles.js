import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    personName : {
        fontSize: 24,
        color: '#000000',
    },
    missingImage : {
        height: 300,
        width: 300,
    }
})

export default styles;